import type { Request, Response } from "express";

function CreateOrderController(req: Request, res: Response) {
    try {

    } catch (err) {
        console.error(err);
    }
}

export { CreateOrderController }