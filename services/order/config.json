{
    "wait_for_db": {
        "retries": 20,
        "delay": 3000
    },
    "cors": {
        "origin": ["http://localhost:3000"],
        "methods": ["GET", "POST", "PATCH", "PUT", "DELETE"]
    },
    "routes": {
        "prefix": "/api/v1/order"
    }
}
